{
  "name": "simplecombat",
  "version": "1.0.0",
  "description": "Generated by [Rojo](https://github.com/rojo-rbx/rojo) 7.4.1.",
  "scripts": {
    "auto": "lune run auto",
    "install": "aftman install && wally install --project-path src",
    "watch": "npm-watch"
  },
  "watch": {
    "test": {
      "patterns": ["src/**/*", ".darklua.json", "default.project.json"],
      "quiet": false,
      "legacyWatch": true,
      "delay": 50,
      "runOnChangeOnly": false
    }
  }
}
